// AndroidManifest.xml - This will be auto-generated by Expo/React Native

// android/app/build.gradle configuration guide:
/**
 * Android Build Configuration for Crapette Mobile Game
 * 
 * To set up your Android development environment:
 * 
 * 1. Install Android SDK:
 *    - Download Android Studio from https://developer.android.com/studio
 *    - Install SDKs for API 30+ (recommended API 33+)
 *    - Install Android Emulator
 * 
 * 2. Set Environment Variables:
 *    - ANDROID_SDK_ROOT: Point to your SDK installation
 *    - JAVA_HOME: Point to your JDK installation
 * 
 * 3. Start Android Emulator:
 *    - Open Android Studio
 *    - Click AVD Manager
 *    - Create or select a virtual device
 *    - Start the emulator
 * 
 * 4. Build and Run:
 *    - npm run android (uses Expo)
 *    - or: expo start --android
 * 
 * 5. Debug with Android Studio:
 *    - adb logcat -s "Crapette"
 *    - or use Android Studio Logcat
 */

// Minimum and Target SDK versions
const MIN_SDK_VERSION = 21;
const TARGET_SDK_VERSION = 33;
const COMPILE_SDK_VERSION = 33;

// App configuration
const PACKAGE_NAME = 'com.arielles38.crapette';
const APP_NAME = 'Crapette';

module.exports = {
  MIN_SDK_VERSION,
  TARGET_SDK_VERSION,
  COMPILE_SDK_VERSION,
  PACKAGE_NAME,
  APP_NAME,
};
